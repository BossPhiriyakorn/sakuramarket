/**
 * CMS — ข้อมูลโหลดจาก API ทั้งหมด (ไม่มี mock)
 * Re-export เฉพาะ types และ helpers จาก marketData
 */
export {
  getParcelsWithRoom,
  getParcelsForRoom,
  type ParcelWithRoom,
  type ItemShopCategory,
} from "../../../data/marketData";
